/**
 * DashboardView
 * Source: The full implementation is in src/LuminaOps.jsx (monolithic prototype).
 * When splitting into individual files, move the DashboardView component here
 * and update imports in OpsApp.jsx accordingly.
 *
 * Props:
 *   setView(viewId: string) — navigate to another admin view
 */
export { DashboardView as default } from '../LuminaOps'
